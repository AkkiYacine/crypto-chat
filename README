use npm start to start the application
open two browser for simulating 2 clients and now you can use the chat application
this application is using nodejs server (express) and websocket for the communication